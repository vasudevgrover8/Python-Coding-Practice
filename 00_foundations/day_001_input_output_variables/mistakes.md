# Day 001 Mistake Log

1. Printed fixed name instead of taking input in Problem 1.
2. Used `x` instead of a descriptive variable name like `age`.
3. Used `abs(side)` unnecessarily in square perimeter.
4. Used `int` where `float` is better for price, temperature, rate, principal, and time.
5. Need to follow lowercase variable naming style in Python.
